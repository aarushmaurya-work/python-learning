#input funtion is used to input something, and it is better to store it in a variable

# age = input("Enter your age : ")
#this is store age as string, because the input funtion takes string by default, you you can use type casting
age = int(input(f"Enter your age : "))
print(age)
