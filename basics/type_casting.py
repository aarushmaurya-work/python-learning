#type casting is a way to change a data type 
age = "17" #this is a string

age  =int(age) #string is converted into integer, and its is now a integer

print(f"{age}, {type(age)}") #this print the age and its data type